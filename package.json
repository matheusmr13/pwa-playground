{
  "name": "pwa-playground",
  "version": "1.0.0",
  "description": "PWA Playground with some experimental features",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "yarn --cwd ./packages/webapp install && yarn --cwd ./packages/server install",
    "build": "./scripts/run.js build",
    "start": "yarn --cwd ./packages/server start:prod",
    "start:local": "./scripts/run.js start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/matheusmr13/pwa-playground.git"
  },
  "author": "Matheus Martins",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/matheusmr13/pwa-playground/issues"
  },
  "homepage": "https://github.com/matheusmr13/pwa-playground#readme",
  "devDependencies": {
    "dotenv": "^8.2.0",
    "prettier": "2.1.1"
  }
}
